fastapi
uvicorn[standard]
numpy==1.26.4 # less then 2 for pandas
pandas==2.1.1  # For data manipulation and normalization
pymongo==4.6.0  # MongoDB driver for Python
motor
fastapi-pagination
python-dotenv==1.0.0  # For environment variable management (optional but recommended)
pycountry
phonenumbers
termcolor
python-multipart